import strings from './Strings';
import questions from "./Questions";
import images from "./Images";

//Condensing the static resources for a single import.

const Resources = {
    strings: strings,
    questions: questions,
    images: images
};

export default Resources;